#include "main.h"

/**
 * _abs - Get absolute
 *@n: The number to check
 *
 * Description: 'function to check for absolute value'
 *
 * Return: absolute value of n
 */

int _abs(int n)
{
if (n < 0)
return (n * (-1));
else
return (n);
}
